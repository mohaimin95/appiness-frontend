import { OrderStatusDirective } from './order-status.directive';

describe('OrderStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderStatusDirective();
    expect(directive).toBeTruthy();
  });
});
